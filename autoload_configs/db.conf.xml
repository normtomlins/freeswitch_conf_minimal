<configuration name="db.conf" description="LIMIT DB Configuration">
  <settings>
    <!--
        <param name="odbc-dsn" value="mariadb://Server=$${db_server};Port=$${db_port};Database=freeswitch;Uid=$${odbc_username};Pwd=$${odbc_password};:"/>
    -->
  </settings>
</configuration>
